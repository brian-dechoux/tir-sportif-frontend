export const ROUTES = {
  BACKEND: {
    AUTHENTICATION: {
      LOGIN: "/authentication/login",
      LOGOUT: "/authentication/logout"
    }
  },
  FRONTEND: {
    DASHBOARD: "/"
  }
};

export const ERRORS = {
  EXPIRED_TOKEN: "AUTH001"
};
