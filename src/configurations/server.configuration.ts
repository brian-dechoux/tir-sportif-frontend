export const ROUTES = {
  BACKEND: {
    AUTHENTICATION: {
      LOGIN: "/authentication/login",
      LOGOUT: "/authentication/logout"
    }
  },
  FRONTEND: {
    DASHBOARD: "/",
    LOGIN: "/login"
  }
};

export const ERRORS = {
  EXPIRED_TOKEN: "AUTH001"
};
